@model HomeViewModel

@await Html.PartialAsync("Carousel")

<h2>Prefered clothes of the week </h2>
<h4>Clients preferences -just for you!</h4>

<div class="row">
    @foreach (var cloth in Model.PreferredClothes)
    {
        @Html.Partial("ClothesSummary", cloth)
    }
</div>